import 'package:flutter/material.dart';

class BibliaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Conteúdo de Pastorais',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}