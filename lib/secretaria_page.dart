import 'package:flutter/material.dart';

class SecretariaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Conteúdo de Secretaria',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}